import { vars } from "../../app/theme.css";
import { style } from "@vanilla-extract/css";

export const arrowIcon = style({
  transform: "rotate(180deg)",
});

export const dashboardLayoutHeader = style({
  fontSize: vars.fontSizes.large,
});
